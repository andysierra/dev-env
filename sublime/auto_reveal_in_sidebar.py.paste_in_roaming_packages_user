import sublime
import sublime_plugin

class AutoRevealInSideBar(sublime_plugin.EventListener):
    def on_activated(self, view):
        window = view.window()
        if window and view.file_name():
            window.run_command("reveal_in_side_bar")
